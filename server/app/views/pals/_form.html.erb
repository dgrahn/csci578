<%= form_for(@pal) do |f| %>
  <% if @pal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pal.errors.count, "error") %> prohibited this pal from being saved:</h2>

      <ul>
      <% @pal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_user_id %><br>
    <%= f.number_field :source_user_id %>
  </div>
  <div class="field">
    <%= f.label :target_user_id %><br>
    <%= f.number_field :target_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
